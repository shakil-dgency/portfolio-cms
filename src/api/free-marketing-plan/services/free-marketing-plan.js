'use strict';

/**
 * free-marketing-plan service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::free-marketing-plan.free-marketing-plan');
